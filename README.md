# Riichi Mahjong YOLO Automatic Score Calculator

This project aims to use Raspberry Pi to run YOLO models to automatically identify Mahjong tiles and calculate the scores.

My Picam broke during testing, so I couldn't test this much, but it should work.

I used custom trained YOLO ncnn models so that it can actually run on Raspberry Pi 4B.
